# -------------------------------
# Server
# -------------------------------
PORT=3000

# -------------------------------
# Database (Supabase)
# -------------------------------
DATABASE_URL=postgresql://postgres.yigrfhdorzndooqswpic:hatno1-dajteZ-xavtuq@aws-0-us-west-2.pooler.supabase.com:5432/postgres

# -------------------------------
# Authentication / Security
# -------------------------------
JWT_SECRET=super-secret-change-me

# Base URL of your backend (when deployed)
APP_BASE_URL=https://10.140.0.2:3000

OTP_EXP_MIN=10

# -------------------------------
# Email
# -------------------------------
MAIL_USER=workdayjarvis@gmail.com
MAIL_PASS="hlow awdr xfac brpl"

# -------------------------------
# Ollama (running on the GCP VM host)
# -------------------------------
# If backend running IN DOCKER, Ollama runs on HOST → use host.docker.internal on Mac,
# But on Linux VMs, you use http://<VM_INTERNAL_IP>:11434 OR http://host.docker.internal:11434 with docker flags.
OLLAMA_BASE_URL=http://host.docker.internal:11434

# -------------------------------
# LLM Models
# -------------------------------
LLM_MODEL=llama3.1:8b-instruct-q4_K_S
EMBED_MODEL=bge-large:latest